# --- General Environment Variables ---
# Set to 'development' or 'production'
NODE_ENV=development

# --- Backend (tasks-service, api-gateway, new services) ---
# Replace with your actual PostgreSQL connection string.
# Example for local development with the k8s/infrastructure/postgresql setup:
# DATABASE_URL="postgresql://user:password@localhost:5432/tasks_db"
# Or for a managed database:
# DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"
DATABASE_URL="postgresql://user:password@postgres:5432/tasks_db" # Example for Kubernetes internal access

# OpenAI API Key for AI chatbot functionality
OPENAI_API_KEY="sk-..."

# URL for the Model Context Protocol (MCP) server
# For local development, this might be http://localhost:8001
MCP_SERVER_URL="http://localhost:8001"

# --- Frontend (.env.local.example content would go here, or be referenced) ---
# For Next.js frontend, variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# NEXT_PUBLIC_BACKEND_URL="http://localhost:8000/api" # Example for local backend access
# NEXT_PUBLIC_WEBSOCKET_URL="ws://localhost:8000/ws" # Example for local websocket access

# --- PostgreSQL Database Credentials (for local/Kubernetes infrastructure setup) ---
# These are used by the k8s/infrastructure/postgresql/postgres-deployment.yaml
POSTGRES_DB=tasks_db
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# --- Redis Configuration (for local/Kubernetes infrastructure setup) ---
# REDIS_HOST=localhost
# REDIS_PORT=6379

# --- Dapr Configuration (for local development using dapr run) ---
# DAPR_GRPC_PORT=50001
# DAPR_HTTP_PORT=3500

# --- OCI Configuration for GitHub Actions (Secrets will be used for CD workflow) ---
# OCI_REGION=us-ashburn-1
# OCI_TENANCY_ID=ocid1.tenancy.oc1..xxxx
# OCI_USER_ID=ocid1.user.oc1..xxxx # Or principal fingerprint if using Workload Identity
# OCI_API_KEY_FINGERPRINT=xx:xx:...:xx
# OCI_API_KEY_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----..." # Entire key content
# OKE_CLUSTER_OCID=ocid1.cluster.oc1..xxxx
